import type { ExternalLinkConfig } from "../types/externalLinkConfig";

export const externalLinkConfig: ExternalLinkConfig = {
	enable: true,
	whitelist: ["b.kemeow.top", "kemeow.top"],
	countdownSeconds: 5,
};
